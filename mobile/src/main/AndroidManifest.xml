<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wbf486.accelerometerextract">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
		
		<!--<uses-permission android:name="android.permission.GET_TASKS" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".MyReceiver">
            <intent-filter>
                <!--<action android:name="android.intent.action.ACTION_VOLUME_UP_BUTTON_PRESSED" />
                <action android:name="android.intent.action.ACTION_VOLUME_DOWN_BUTTON_PRESSED" />-->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.ACTION_SLIDER_OFF_BUTTON_PRESSED" />
                <action android:name="android.intent.action.ACTION_SLIDER_ON_BUTTON_PRESSED" />
                <action android:name="android.intent.action.ACTION_ORANGE_BUTTON_PRESSED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".MainService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".DeviceListActivity"
            android:label="@string/select_device"
            android:theme="@android:style/Theme.Holo.Dialog" />
        
        <activity
            android:name=".BluetoothDialog"
            android:label="@string/bt_dialog"
            android:theme="@android:style/Theme.Dialog" />

        <activity
            android:name=".GunShotDetected"
            android:theme="@android:style/Theme.Dialog" />

        <activity android:name=".MainActivity"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>